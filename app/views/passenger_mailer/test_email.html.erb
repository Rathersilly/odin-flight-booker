<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>

    <h3>Welcome<%=  @passenger %></h3>
    <p>You have booked a ticket</p>
    <p>flight: <%= @flight%></p>
    <p>from: <%= @flight.from_airport.name %></p>
    <p>to: <%= @flight.to_airport.name %></p>
    <br>
    <p>takeoff date/time: <%= @flight.start %></p>
    <p>flight duration: <%= @flight.duration %></p>
    <br>
    <p>The flight's page is here: <%= @flight_url %></p>
    <p>The flight's page is here: <%# link_to @flight_url %></p>
    <p>The flight's page is here: <%# link_to flight_url(@flight.id) %></p>
    <p>The flight's page is here: <%= link_to "uhhhhh",flight_url(@flight.id) %></p>
    <p>The flight's page is here: <%= link_to @flight_url, @flight_url %></p>
    <%# Why does this give error message when only one argument? %>
    <p>The flight's page is here: <%# link_to @flight_url%></p>

  </body>
</html>

